# config.py: Configuration values shared by the whole project.

# Character used in the input files to mark a cell that contains a thief.
THIEF = "!"

# Arian can only walk down or right, so every node has at most two successors.
DIRECTIONS = [(1, 0), (0, 1)]
